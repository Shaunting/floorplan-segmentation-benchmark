# UNet-ResNet50 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement a UNet with a pretrained ResNet50 encoder that produces 44-channel floor plan segmentation outputs compatible with the existing CubiCasa5k training and evaluation pipeline.

**Architecture:** ResNet50 encoder (torchvision, ImageNet pretrained) tapped at four stages (layer1–layer4) feeds a 3-stage symmetric decoder using ConvTranspose2d + DoubleConv blocks with skip connections. The decoder stops at 1/4 input resolution, then a `1×1 Conv → 4× ConvTranspose2d` head brings output to full resolution — matching the Furukawa model's output convention. The first 21 output channels (junction heatmaps) are passed through `sigmoid`; the remaining 23 (rooms + icons) are raw logits, identical to both existing models.

**Tech Stack:** PyTorch, torchvision (ResNet50), existing `DoubleConv` pattern from `floortrans/models/unet.py`

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `floortrans/models/unet_resnet50.py` | Create | `UNetResNet50` model class |
| `floortrans/models/__init__.py` | Modify | Register `unet_resnet50` in `get_model()` |

---

## Task 1: Implement `UNetResNet50`

**Files:**
- Create: `floortrans/models/unet_resnet50.py`

### Architecture detail

**Encoder (ResNet50 stages):**

| Name | Source | Resolution | Channels |
|---|---|---|---|
| stem | `conv1 → bn1 → relu → maxpool` | H/4 | 64 (not used as skip) |
| `enc1` | `layer1` | H/4 | 256 |
| `enc2` | `layer2` | H/8 | 512 |
| `enc3` | `layer3` | H/16 | 1024 |
| `enc4` (bottleneck) | `layer4` | H/32 | 2048 |

**Decoder:**

| Stage | Up in channels | Skip channels | Concat channels | Out channels |
|---|---|---|---|---|
| `dec3` | 2048 → 1024 (ConvTranspose 2×) | 1024 (enc3) | 2048 | 1024 |
| `dec2` | 1024 → 512 (ConvTranspose 2×) | 512 (enc2) | 1024 | 512 |
| `dec1` | 512 → 256 (ConvTranspose 2×) | 256 (enc1) | 512 | 256 |

**Head (Furukawa-aligned):**
- `Conv2d(256, n_classes, kernel_size=1)` at H/4
- `ConvTranspose2d(n_classes, n_classes, kernel_size=4, stride=4)` → H/1 (full res)
- `sigmoid` on `out[:, :21]`

- [ ] **Step 1: Write a smoke test that verifies output shape and heatmap range**

Create `tests/test_unet_resnet50.py`:

```python
import torch
import pytest
from floortrans.models.unet_resnet50 import UNetResNet50


def test_output_shape_256():
    model = UNetResNet50(n_classes=44)
    model.eval()
    x = torch.zeros(1, 3, 256, 256)
    with torch.no_grad():
        out = model(x)
    assert out.shape == (1, 44, 256, 256), f"Expected (1,44,256,256) got {out.shape}"


def test_heatmap_channels_sigmoid():
    """First 21 channels must be in [0, 1] (sigmoid applied)."""
    model = UNetResNet50(n_classes=44)
    model.eval()
    x = torch.randn(1, 3, 256, 256)
    with torch.no_grad():
        out = model(x)
    heatmaps = out[0, :21]
    assert heatmaps.min() >= 0.0, "Heatmap min below 0"
    assert heatmaps.max() <= 1.0, "Heatmap max above 1"


def test_non_heatmap_channels_unbounded():
    """Channels 21-43 must NOT be clamped to [0,1]."""
    model = UNetResNet50(n_classes=44)
    model.eval()
    x = torch.randn(2, 3, 256, 256)
    with torch.no_grad():
        out = model(x)
    rest = out[:, 21:]
    # With random weights the raw logits should have values outside [0, 1]
    assert rest.min() < 0.0 or rest.max() > 1.0, "Non-heatmap channels appear sigmoid'd"


def test_odd_input_size():
    """_align_and_concat must handle spatial sizes that are not multiples of 32."""
    model = UNetResNet50(n_classes=44)
    model.eval()
    x = torch.zeros(1, 3, 240, 320)
    with torch.no_grad():
        out = model(x)
    assert out.shape[0] == 1
    assert out.shape[1] == 44
```

- [ ] **Step 2: Run the test to confirm it fails (module not yet implemented)**

```bash
cd /path/to/CubiCasa5k
python -m pytest tests/test_unet_resnet50.py -v
```

Expected: `ModuleNotFoundError` or `ImportError` — the file doesn't exist yet.

- [ ] **Step 3: Implement `unet_resnet50.py`**

Write `floortrans/models/unet_resnet50.py` with the following complete implementation:

```python
import torch
import torch.nn as nn
import torch.nn.functional as F
from torchvision.models import resnet50, ResNet50_Weights


class DoubleConv(nn.Module):
    def __init__(self, in_ch, out_ch):
        super().__init__()
        self.net = nn.Sequential(
            nn.Conv2d(in_ch, out_ch, 3, padding=1, bias=False),
            nn.BatchNorm2d(out_ch),
            nn.ReLU(inplace=True),
            nn.Conv2d(out_ch, out_ch, 3, padding=1, bias=False),
            nn.BatchNorm2d(out_ch),
            nn.ReLU(inplace=True),
        )

    def forward(self, x):
        return self.net(x)


class UNetResNet50(nn.Module):
    def __init__(self, n_classes):
        super().__init__()

        backbone = resnet50(weights=ResNet50_Weights.IMAGENET1K_V1)

        # Encoder — tap ResNet50 at four stages
        self.stem   = nn.Sequential(backbone.conv1, backbone.bn1,
                                    backbone.relu, backbone.maxpool)
        self.enc1   = backbone.layer1   # H/4,  256ch
        self.enc2   = backbone.layer2   # H/8,  512ch
        self.enc3   = backbone.layer3   # H/16, 1024ch
        self.enc4   = backbone.layer4   # H/32, 2048ch  (bottleneck)

        # Decoder
        self.up3    = nn.ConvTranspose2d(2048, 1024, kernel_size=2, stride=2)
        self.dec3   = DoubleConv(2048, 1024)   # 1024 up + 1024 skip

        self.up2    = nn.ConvTranspose2d(1024, 512, kernel_size=2, stride=2)
        self.dec2   = DoubleConv(1024, 512)    # 512 up + 512 skip

        self.up1    = nn.ConvTranspose2d(512, 256, kernel_size=2, stride=2)
        self.dec1   = DoubleConv(512, 256)     # 256 up + 256 skip

        # Furukawa-aligned head: 1×1 conv at H/4, then 4× upsample to full res
        self.head   = nn.Conv2d(256, n_classes, kernel_size=1)
        self.upsample = nn.ConvTranspose2d(n_classes, n_classes,
                                           kernel_size=4, stride=4)

        self._init_decoder_weights()

    # -----------------------------------------------------------------------
    # Safe skip connection — handles odd spatial sizes (same as vanilla UNet)
    # -----------------------------------------------------------------------
    def _align_and_concat(self, x, skip):
        _, _, H, W = skip.shape
        x = F.interpolate(x, size=(H, W), mode='bilinear', align_corners=False)
        return torch.cat([x, skip], dim=1)

    def forward(self, x):
        # Encoder
        s   = self.stem(x)     # H/4,  64ch  (not used as skip)
        e1  = self.enc1(s)     # H/4,  256ch
        e2  = self.enc2(e1)    # H/8,  512ch
        e3  = self.enc3(e2)    # H/16, 1024ch
        e4  = self.enc4(e3)    # H/32, 2048ch

        # Decoder
        d3  = self.up3(e4)
        d3  = self.dec3(self._align_and_concat(d3, e3))

        d2  = self.up2(d3)
        d2  = self.dec2(self._align_and_concat(d2, e2))

        d1  = self.up1(d2)
        d1  = self.dec1(self._align_and_concat(d1, e1))

        # Head (Furukawa-aligned: predict at H/4, then 4× upsample)
        out = self.upsample(self.head(d1))

        # Match both existing models: heatmap channels through sigmoid
        out = out.clone()
        out[:, :21] = torch.sigmoid(out[:, :21])
        return out

    def _init_decoder_weights(self):
        decoder_modules = [
            self.up3, self.dec3,
            self.up2, self.dec2,
            self.up1, self.dec1,
            self.head, self.upsample,
        ]
        for module in decoder_modules:
            for m in module.modules():
                if isinstance(m, (nn.Conv2d, nn.ConvTranspose2d)):
                    nn.init.kaiming_normal_(m.weight, mode='fan_out',
                                           nonlinearity='relu')
                    if m.bias is not None:
                        nn.init.constant_(m.bias, 0)
                elif isinstance(m, nn.BatchNorm2d):
                    nn.init.constant_(m.weight, 1)
                    nn.init.constant_(m.bias, 0)
```

- [ ] **Step 4: Run the tests and verify they all pass**

```bash
python -m pytest tests/test_unet_resnet50.py -v
```

Expected output (all four pass):
```
PASSED tests/test_unet_resnet50.py::test_output_shape_256
PASSED tests/test_unet_resnet50.py::test_heatmap_channels_sigmoid
PASSED tests/test_unet_resnet50.py::test_non_heatmap_channels_unbounded
PASSED tests/test_unet_resnet50.py::test_odd_input_size
```

- [ ] **Step 5: Commit**

```bash
git add floortrans/models/unet_resnet50.py tests/test_unet_resnet50.py
git commit -m "feat: add UNetResNet50 model with ImageNet-pretrained encoder"
```

---

## Task 2: Register model in `get_model()`

**Files:**
- Modify: `floortrans/models/__init__.py`

- [ ] **Step 1: Write a test for `get_model` dispatch**

Add to `tests/test_unet_resnet50.py`:

```python
from floortrans.models import get_model


def test_get_model_dispatches_unet_resnet50():
    model = get_model('unet_resnet50', n_classes=44)
    x = torch.zeros(1, 3, 256, 256)
    with torch.no_grad():
        out = model(x)
    assert out.shape == (1, 44, 256, 256)


def test_get_model_unknown_arch_raises():
    with pytest.raises(ValueError, match="not available"):
        get_model('nonexistent_arch', n_classes=44)
```

- [ ] **Step 2: Run to confirm `test_get_model_dispatches_unet_resnet50` fails**

```bash
python -m pytest tests/test_unet_resnet50.py::test_get_model_dispatches_unet_resnet50 -v
```

Expected: `FAILED` — `get_model('unet_resnet50', ...)` raises `ValueError`.

- [ ] **Step 3: Update `floortrans/models/__init__.py`**

The file already registers `unet`, `segformer`, and `deeplab`. Make two targeted additions only — do NOT replace the file.

Add the import after the existing imports at the top:

```python
from floortrans.models.unet_resnet50 import UNetResNet50
```

Add the `elif` branch before the `else` block:

```python
    elif name == 'unet_resnet50':
        model = UNetResNet50(n_classes=n_classes)
```

The final file should look like:

```python
from floortrans.models.hg_furukawa_original import *
from floortrans.models.unet import *
from floortrans.models.segformer import *
from floortrans.models.deeplab import DeepLabV3Plus
from floortrans.models.unet_resnet50 import UNetResNet50


def get_model(name, n_classes=None, version=None):

    if name == 'hg_furukawa_original':
        model = hg_furukawa_original(n_classes=n_classes)
        model.init_weights()

    elif name == 'unet':
        model = UNet(n_classes=n_classes)

    elif name == 'segformer':
        model = segFormer(n_classes=n_classes)

    elif name == 'deeplab':
        model = DeepLabV3Plus(n_classes=n_classes)

    elif name == 'unet_resnet50':
        model = UNetResNet50(n_classes=n_classes)

    else:
        raise ValueError('Model {} not available'.format(name))

    return model
```

- [ ] **Step 4: Run all tests**

```bash
python -m pytest tests/test_unet_resnet50.py -v
```

Expected: all 6 tests pass.

- [ ] **Step 5: Quick end-to-end smoke check (no GPU needed)**

```bash
python - <<'EOF'
import torch
from floortrans.models import get_model

model = get_model('unet_resnet50', n_classes=44)
model.eval()
x = torch.zeros(1, 3, 256, 256)
with torch.no_grad():
    out = model(x)
print("Output shape:", out.shape)
print("Heatmap range: [{:.4f}, {:.4f}]".format(out[0, :21].min().item(), out[0, :21].max().item()))
print("OK")
EOF
```

Expected:
```
Output shape: torch.Size([1, 44, 256, 256])
Heatmap range: [0.5000, 0.5000]
OK
```

- [ ] **Step 6: Commit**

```bash
git add floortrans/models/__init__.py tests/test_unet_resnet50.py
git commit -m "feat: register unet_resnet50 and unet in get_model()"
```

---

## Task 3: Verify training compatibility

This task confirms the model slots into `train.py` without modification.

**Files:** No code changes — this is a verification step.

- [ ] **Step 1: Dry-run one training step in debug mode**

On the training machine (with GPU and dataset available):

```bash
python train.py \
  --arch unet_resnet50 \
  --n-classes 44 \
  --batch-size 2 \
  --image-size 256 \
  --n-epoch 1 \
  --data-path data/cubicasa5k/ \
  --debug
```

Expected: training completes 1 epoch without error. TensorBoard graph is written. Checkpoint saved to `runs_cubi/<timestamp>/model_last_epoch.pkl`.

- [ ] **Step 2: Confirm checkpoint can be reloaded**

```bash
python - <<'EOF'
import torch
from floortrans.models import get_model

# Replace with the actual checkpoint path printed during training
ckpt_path = "runs_cubi/<timestamp>/model_last_epoch.pkl"
checkpoint = torch.load(ckpt_path, map_location='cpu')
model = get_model('unet_resnet50', n_classes=44)
model.load_state_dict(checkpoint['model_state'])
print("Checkpoint loaded successfully.")
EOF
```

- [ ] **Step 3: Commit nothing (verification only)**

If Step 1 or 2 fail, fix the root cause in `unet_resnet50.py` or `__init__.py` and amend the relevant commit.

---

## Summary

| Task | Files touched | Outcome |
|---|---|---|
| 1 | `floortrans/models/unet_resnet50.py` | Model implemented and tested |
| 2 | `floortrans/models/__init__.py` | Registered; `--arch unet_resnet50` works |
| 3 | None | Verified end-to-end with `train.py` |
